// Socket.IO stub — not needed for local desktop app
// Maintains the same interface so existing code doesn't break

export function initSocket(_token: string, _meseroId?: string): any {
  return null
}

export function getSocket(): any {
  return null
}

export function disconnectSocket() {
  // no-op
}
